import { createSlice } from "@reduxjs/toolkit";

export const cuponesSlice = createSlice({
    name:'cupones',
    initialState:{
        isLoading:true,
    },
    reducers:{}
});

export const {} = cuponesSlice.actions